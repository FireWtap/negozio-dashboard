import { defineNuxtPlugin } from "#app";

export default defineNuxtPlugin((nuxtApp) => {
  // This plugin is intentionally minimal to avoid SSR issues
  // Naive UI components will work without additional setup
});
